[project]
name = "blisscafe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.2",
]

[tool.uv]
dev-dependencies = [
    "pyright>=1.1.385",
    "ruff>=0.7.0",
]
